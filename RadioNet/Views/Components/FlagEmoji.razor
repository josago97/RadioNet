@GetEmoji()

@code {
    [Parameter] public string CountryCode { get; set; }

    private string GetEmoji()
    {
        string countryCode = CountryCode.ToUpper();

        return string.Concat(countryCode.Select(x => char.ConvertFromUtf32(x + 0x1F1A5)));
    }
}
